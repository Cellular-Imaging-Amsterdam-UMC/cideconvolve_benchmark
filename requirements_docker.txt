# ===========================================================================
# Docker-specific requirements for CIDeconvolve benchmark
# ===========================================================================
# PyTorch CUDA wheels are pulled via --extra-index-url (see Dockerfile).
# All optional GPU packages are enabled here since the Docker image ships
# with NVIDIA CUDA runtime + OpenCL support.
# All versions pinned for reproducible builds.
# ===========================================================================

# PyTorch with CUDA 12.6 support
--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.4.1
torchvision==0.19.1

# Core dependencies
numpy==2.4.3
tifffile==2025.5.21
ome-types==0.6.3
scipy==1.15.3
Pillow==10.4.0
scikit-image==0.24.0

# OME-TIFF reading
bioio==3.2.0
bioio-ome-tiff==1.4.0

# PSF generation (vendored sdeconv & psf_generator use these)
zernikepy==0.0.5

# RedLionfish RL deconvolution (OpenCL GPU + CPU fallback)
redlionfish==0.10

# cuCIM GPU-accelerated Richardson-Lucy (Linux only, NVIDIA GPU)
cucim-cu12==26.2.1
cupy-cuda12x==14.0.1

# System metrics during benchmark
psutil==7.0.0
nvidia-ml-py==12.575.51
