# ===========================================================================
# Python version: 3.10 or 3.11 recommended (3.10+ required by bioio)
# ===========================================================================
#
# --- Install PyTorch with CUDA GPU support FIRST ---
#
# PyTorch's default pip package is CPU-only. You must install the CUDA build
# separately before running: pip install -r requirements.txt
#
# For CUDA 13.x (driver 13.2+):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu132
#
# If the cu132 wheel is not yet available, use the latest compatible build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# Check your CUDA version: nvidia-smi
# Verify GPU is working: python -c "import torch; print(torch.cuda.is_available())"
#
# ===========================================================================

# Core dependencies
numpy>=1.23.0
tifffile>=2023.1.0
ome-types>=0.5.0
scipy>=1.10.0
Pillow>=9.0.0
scikit-image>=0.20.0

# OME-TIFF reading
bioio>=1.0.0
bioio-ome-tiff>=1.0.0

# PSF generation and sdeconv deconvolution are vendored in vendor/
# (no external install needed). Their dependencies:
zernikepy>=0.1.0

# Optional: RedLionfish RL deconvolution (OpenCL GPU + CPU fallback)
# redlionfish

# Optional: CUDA-accelerated Richardson-Lucy (requires NVIDIA GPU)
# Install via conda: conda install -c conda-forge pycudadecon
# pycudadecon

# Optional: cuCIM GPU-accelerated RL (Linux only, requires NVIDIA GPU)
# pip install cucim-cu12 cupy-cuda12x

# System metrics during benchmark
psutil>=5.9.0
# nvidia-ml-py  # optional: GPU metrics via pynvml (auto-detected at runtime)
